; Grading function using Nested if in Metta
(= (grade $score)
(if (> $score 90) 
 (Graded as A)
   (if (> $score 80)
     (Graded as B)
       (if (> $score 70)
         (Graded as C)
         (Fail)
          ))))

! (grade 90)
